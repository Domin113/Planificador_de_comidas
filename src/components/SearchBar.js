// Componente para la barra de búsqueda.
import React from 'react';

const SearchBar = ({ placeholder }) => {
  return (
    <input type="text" placeholder={placeholder} />
  );
};

export default SearchBar;  // Asegúrate de tener esta línea